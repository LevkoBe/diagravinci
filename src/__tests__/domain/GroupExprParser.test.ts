// GroupExprParser has been removed — group rules are now plain regex strings.
// Matching tests live in GroupEvaluator.test.ts.
import { describe, it } from "vitest";
describe("GroupExprParser (removed)", () => {
  it("is superseded by plain regex group rules", () => {
    // No parser to test — see GroupEvaluator.test.ts
  });
});
